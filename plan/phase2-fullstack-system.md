# 阶段二：全栈深入 + 系统思维（Day 21-45）

> 目标：完善 bookmark 全栈功能，培养系统设计和架构思维
>
> 节奏：周一-周四实战，周五原理日，周六挑战日，周日视野日

---

## 第 4 周（Day 21-27）：进阶功能 + 性能原理

### Day 21（周一）— 实时同步：WebSocket 🔨

#### 🧠 独立思考（30 分钟）

**拆解训练**：任务是"让 bookmark 支持实时同步——用户在手机上添加高亮，电脑上实时出现"

**自己拆解**（用四步法），然后展开对比：

<details>
<summary>参考拆解</summary>

```
终点：多设备间的笔记/高亮实时同步

材料：
  - WebSocket 服务端（NestJS Gateway）
  - WebSocket 客户端（Socket.io-client）
  - 事件定义：哪些操作需要实时推送？
  - 房间设计：按用户还是按书籍分房间？

顺序：
  1. 先搞清楚什么数据需要实时同步，什么轮询就够
     - 实时：笔记/高亮的增删改（多设备协作场景）
     - 轮询：阅读进度（不需要那么及时）
  2. NestJS 安装 @nestjs/websockets + socket.io
  3. 创建 SyncGateway，定义事件
  4. 前端连接 WebSocket，监听事件
  5. 处理断连重连

验证：
  - 开两个浏览器标签，登录同一账号
  - 在 A 标签加一条高亮，B 标签是否实时出现？
```
</details>

**关键决策（自己想）**：
- 什么数据需要实时同步？什么数据轮询就够了？（提示：不是所有数据都值得 WebSocket）
- WebSocket 连接断了怎么办？重连策略怎么设计？（指数退避？最大重试次数？）
- 房间设计：每个用户一个房间？还是每本书一个房间？

#### 🤖 AI 辅助（40 分钟）

- 让 AI 生成 NestJS WebSocket Gateway（Socket.io）
- 让 AI 生成前端 Socket.io-client 的连接管理 Hook（含断连重连）
- 让 AI 实现"新增高亮后广播给同一用户的其他设备"

#### 👀 Review 清单

- [ ] WebSocket 连接是否需要认证？（token 怎么传？通过 handshake query 还是第一条消息？）
- [ ] 是否处理了连接断开和重连？
- [ ] 广播时是否排除了发送者自己？（不然自己加的高亮会收到两次）
- [ ] 前端收到 WebSocket 消息后，是否正确更新了本地状态？

---

### Day 22（周二）— 分享功能 🔨

#### 🧠 独立思考（25 分钟）

**拆解训练**：任务是"让用户可以生成笔记/高亮的公开分享链接"

**自己先想清楚**：
- 分享链接长什么样？`/share/abc123` 这个 `abc123` 怎么生成？
- 谁能看到分享内容？公开？需要密码？只有链接的人能看？
- 分享页面需要登录吗？（不需要！否则就不叫"分享"了）
- 分享的数据怎么存？新建 Share 表？还是在 Note 表加 shareId 字段？

<details>
<summary>参考拆解</summary>

```
终点：用户能生成一个链接，别人打开就能看到笔记/高亮

材料：
  - Share 模型：id, shareId(唯一短串), type(note/highlight/collection),
    targetId, accessLevel(public/password), password?, userId, createdAt, expiresAt?
  - API：POST /shares（创建分享）、GET /shares/:shareId（访问分享内容）
  - 前端：分享弹窗（选择权限、复制链接）+ 分享查看页面

顺序：
  1. 设计 Share 数据模型
  2. 实现创建分享 API（生成 nanoid 短链接）
  3. 实现查看分享 API（不需要登录）
  4. 前端分享弹窗组件
  5. 前端分享查看页面

验证：
  - 创建分享 → 拿到链接 → 退出登录 → 打开链接 → 能看到内容
  - 密码保护的分享 → 输对密码才能看
```
</details>

#### 🤖 AI 辅助（35 分钟）

- 让 AI 生成 Share 模块（Model + Controller + Service）
- 让 AI 生成前端分享弹窗和分享查看页面

#### 👀 Review 清单

- [ ] shareId 是否足够随机？（不能用自增 ID，容易被遍历）
- [ ] 分享查看接口是否真的不需要登录？
- [ ] 密码保护的分享，密码是否加密存储？
- [ ] 是否有过期机制？（链接不应该永远有效）

---

### Day 23（周三）— API 版本管理 + 文档 🔨

#### 🧠 独立思考（20 分钟）

**思考这些问题**（先自己想）：
- 为什么 API 需要版本？想象这个场景：你改了 `/books` 的返回格式，但用户手机上的旧版 App 还在用旧格式——怎么办？
- 版本号放在哪？URL 路径（`/api/v1/`）？Header（`Accept: application/vnd.api.v1+json`）？Query（`?version=1`）？各有什么优缺点？
- Swagger 文档怎么从代码自动生成？（NestJS 的装饰器如何映射成文档）

#### 🤖 AI 辅助（40 分钟）

- 让 AI 给所有 API 加上 `/api/v1/` 前缀
- 让 AI 配置 `@nestjs/swagger`，自动生成 API 文档
- 让 AI 设计统一的错误响应格式：`{ code: number, message: string, details?: any }`

#### 👀 Review 清单

- [ ] Swagger 页面能正常访问？（通常在 `/api/docs`）
- [ ] 每个接口的请求/响应类型标注是否完整？
- [ ] 错误码是否有规范？（比如 10001=参数错误, 20001=认证失败, 30001=资源不存在）
- [ ] 生产环境是否应该关闭 Swagger？（安全考虑）

---

### Day 24（周四）— 前端重构：状态管理优化 🔨

#### 🧠 独立思考（30 分钟）

**拆解训练**：任务是"优化 bookmark 前端的状态管理"

**先搞清楚概念**：
- **本地状态**：只在当前组件用的（表单输入值、弹窗开关）→ `useState`
- **客户端全局状态**：多个组件共享的前端状态（主题、侧边栏开关）→ Zustand
- **服务端状态**：来自 API 的数据（书籍列表、笔记）→ React Query / SWR

**自己审查 bookmark 代码**：
1. 打开 Zustand store，哪些数据其实是"服务端状态"？（如 books、notes）
2. 这些数据用 Zustand 管理有什么问题？（缓存失效？loading 状态？重复请求？）
3. 如果改用 React Query，哪些问题会自动解决？

#### 🤖 AI 辅助（40 分钟）

- 让 AI 把 bookmark 的服务端状态从 Zustand 迁移到 React Query
- 让 AI 配置 React Query 的缓存策略（staleTime、refetchOnWindowFocus）

#### 👀 Review 清单

- [ ] Zustand 里是否只剩下纯客户端状态了？
- [ ] React Query 的 queryKey 设计是否合理？（比如 `['books', userId]`）
- [ ] loading/error 状态是否在 UI 上正确展示？
- [ ] stale-while-revalidate 策略是否理解？（先显示缓存，后台刷新）

---

### Day 25（周五）— 原理日：浏览器渲染与性能 📖

#### 🧠 独立思考（全天，60-90 分钟）

今天不写代码，纯学原理。

**学习方式**：先自己带着问题去查资料，而不是问 AI

**带着这些问题去学**：

1. **关键渲染路径**（25 分钟）：
   - 浏览器拿到 HTML 后，到页面显示出来，中间经历了哪些步骤？
   - HTML → DOM → CSSOM → Render Tree → Layout → Paint → Composite
   - 每一步在做什么？哪一步最耗时？

2. **重排 vs 重绘**（15 分钟）：
   - 什么 CSS 属性会触发重排（reflow）？什么只触发重绘（repaint）？
   - 为什么说"读写分离 DOM 操作"能提升性能？
   - `transform` 和 `opacity` 为什么性能好？（提示：它们只触发 Composite）

3. **Web Vitals**（15 分钟）：
   - LCP（最大内容绘制）/ FID（首次输入延迟）/ CLS（累积布局偏移）分别是什么？
   - 打开 bookmark，用 Lighthouse 跑一次，三个指标分别多少？

4. **React 渲染机制**（15 分钟）：
   - 虚拟 DOM diff 是怎么工作的？
   - `key` 属性为什么重要？不加会怎样？
   - 什么时候用 `React.memo`？什么时候不该用？

**实践**：
- 用 Chrome DevTools Performance 面板录制 bookmark 的一次操作
- 截图标注：哪些是 Layout？哪些是 Paint？哪些是 JS 执行？

#### 🤖 AI 使用方式

- 你自己查完资料、写完笔记后，让 AI 帮你检查理解是否正确
- 有不确定的地方可以问 AI，但先自己想过再问

#### 产出

- 一篇博客笔记《浏览器渲染原理——前端性能优化的基础》
- 包含 Lighthouse 截图和 Performance 面板分析

---

### Day 26（周六）— 挑战日：优化一个真实性能问题 🧪

#### 🧠 完全独立完成（60 分钟）

**规则**：关掉所有 AI 工具。可以查文档，但不能用 AI 对话。

**任务**：
1. 在 bookmark 项目中找一个性能瓶颈（至少一个）
   - 可能的方向：大列表渲染、频繁重渲染、大文件加载、图片未优化
2. 用 DevTools 量化问题（截图记录优化前的数据）
3. 自己分析原因并修复
4. 再次量化，对比优化前后的差异

**拆解提示**：

<details>
<summary>先自己想怎么找到性能问题</summary>

```
1. 打开 Performance 面板，录制一段操作
2. 看火焰图，找最长的色块——那就是瓶颈
3. 常见问题：
   - 列表没有虚拟滚动 → 几百个 DOM 节点
   - 组件频繁重渲染 → React Profiler 看哪个组件渲染次数多
   - 图片太大 → Network 面板看传输大小
4. 针对性修复：
   - 虚拟滚动：react-window / react-virtuoso
   - 减少重渲染：React.memo / useMemo / useCallback
   - 图片优化：压缩 / 懒加载 / WebP 格式
```
</details>

**写完后**：打开 AI，对比你的优化方案和 AI 的建议

---

### Day 27（周日）— 视野日 🌍

#### 🧠 独立思考

研究 3 款优秀的阅读类产品（推荐：Readwise / Omnivore / Raindrop），从全栈开发者视角思考：

对每个产品回答：
1. 它的核心差异化是什么？技术上怎么实现的？
2. 哪些功能设计让你觉得"这个巧妙"？
3. bookmark 可以借鉴什么？列出 3 个具体的改进点

#### 产出

- 一篇博客笔记《从全栈视角拆解 3 款阅读产品》

---

## 第 5 周（Day 28-34）：工程化 + Node.js 原理

### Day 28（周一）— monorepo 重构 🔨

#### 🧠 独立思考（25 分钟）

**拆解训练**：任务是"把 bookmark 重构为 pnpm monorepo"

**先搞清楚为什么要这么做**：
- 现在前端和后端是两个独立项目，共享类型怎么办？复制粘贴？
- 如果用 monorepo，前后端可以共享同一份 TypeScript 类型定义
- 前后端还能共享校验规则（zod schema）——注册接口的邮箱格式校验，前后端写一遍就够

**自己设计目录结构**：

<details>
<summary>参考结构</summary>

```
bookmark/
  packages/
    shared/      — 共享 TS 类型 + zod 校验规则
    server/      — NestJS 后端
    web/         — React 前端
  pnpm-workspace.yaml
  package.json   — 根目录的 scripts
  tsconfig.base.json — 共享的 TS 配置
```
</details>

**思考**：
- monorepo vs polyrepo 各有什么优缺点？
- 什么时候不应该用 monorepo？

#### 🤖 AI 辅助（45 分钟）

- 让 AI 配置 pnpm workspace
- 让 AI 把前后端共享的类型提取到 shared 包
- 让 AI 用 zod 统一前后端的校验规则

#### 👀 Review 清单

- [ ] `pnpm-workspace.yaml` 配置正确？
- [ ] shared 包能在 server 和 web 中正确引用？（`@bookmark/shared`）
- [ ] 共享类型是否只包含数据类型，不包含业务逻辑？
- [ ] zod schema 是否在前后端都能正确运行？

---

### Day 29（周二）— 测试策略设计 🔨

#### 🧠 独立思考（25 分钟）

**拆解训练**：任务是"给 bookmark 制定测试策略"

**先理解测试金字塔**：
```
          /  E2E  \        ← 少，慢，贵（Playwright）
         / 集成测试 \       ← 中等（API 测试，组件测试）
        /  单元测试   \     ← 多，快，便宜（Vitest）
```

**自己决策**（写在纸上）：
- bookmark 的哪些代码最值得测试？（ROI 思维：改动频繁 + 出 bug 影响大的）
- 哪些代码不值得测试？（纯 UI 展示、第三方库的封装）
- 具体来说：
  - 后端：认证流程（注册/登录/token 验证）→ 集成测试
  - 后端：核心业务逻辑（CRUD、搜索、同步）→ 集成测试
  - 前端：关键交互（登录表单、书籍列表、笔记编辑）→ 组件测试
  - shared：zod 校验规则 → 单元测试

#### 🤖 AI 辅助（40 分钟）

- 让 AI 配置 Vitest + 测试工具
- 让 AI 为认证 API 写集成测试（用 supertest）
- 让 AI 为 zod schema 写单元测试

#### 👀 Review 清单

- [ ] 测试是否真的在测有价值的东西？（不是为了覆盖率而测试）
- [ ] 集成测试是否用了真实数据库？（不要 mock 数据库，用测试数据库）
- [ ] 测试之间是否互相独立？（不依赖执行顺序）
- [ ] 测试数据是否在每次测试前后清理？

---

### Day 30（周三）— 错误监控与日志 🔨

#### 🧠 独立思考（20 分钟）

**思考这些问题**：
- 线上出 bug 了，你怎么知道？用户告诉你？还是你先发现？
- 如果有 100 个用户同时遇到问题，你怎么快速定位是哪个接口、哪行代码出了问题？
- 日志应该记什么？不应该记什么？（密码、token 绝对不能记！）

**自己设计日志方案**：
- 每个请求需要一个唯一 requestId，方便追踪
- 日志格式：`[时间] [级别] [requestId] [消息] [额外数据]`
- 前端的错误怎么上报到后端？

#### 🤖 AI 辅助（40 分钟）

- 让 AI 配置后端结构化日志（Pino / Winston）
- 让 AI 实现请求级别的 requestId 追踪
- 让 AI 配置前端错误捕获（ErrorBoundary + window.onerror）
- 让 AI 接入 Sentry 免费版（或实现简单的错误上报接口）

#### 👀 Review 清单

- [ ] 日志中是否泄露了敏感信息？（密码、token、用户隐私数据）
- [ ] requestId 是否贯穿了整个请求链路？
- [ ] 前端 ErrorBoundary 是否有友好的降级 UI？
- [ ] 日志级别是否合理？（info / warn / error 各在什么时候用）

---

### Day 31（周四）— 缓存策略 🔨

#### 🧠 独立思考（25 分钟）

**拆解训练**：任务是"给 bookmark 加缓存"

**先理解缓存的三个层面**：

1. **HTTP 缓存**：浏览器自动缓存静态资源（JS/CSS/图片）
   - `Cache-Control: max-age=31536000`（一年）+ 文件名带 hash
   - 问自己：API 响应应该缓存吗？（大部分不应该）

2. **API 缓存**：后端缓存热点查询结果
   - 书籍列表很少变化 → 缓存 5 分钟
   - 用户改了数据 → 怎么让缓存失效？

3. **前端缓存**：React Query 的缓存 + Service Worker 离线缓存
   - staleTime 设多久？0（总是重新验证）？5 分钟？
   - 离线时显示缓存数据还是显示"无网络"？

**关键思考**：缓存最难的不是"怎么加"，而是"什么时候该失效"

#### 🤖 AI 辅助（35 分钟）

- 让 AI 为静态资源配置 HTTP 缓存头
- 让 AI 在 NestJS 中实现简单的内存缓存（CacheModule）
- 让 AI 优化 Service Worker 的缓存策略

#### 👀 Review 清单

- [ ] 静态资源是否用了 content hash 文件名？
- [ ] API 缓存是否设置了合理的 TTL？
- [ ] 用户修改数据后，缓存是否会失效？
- [ ] Service Worker 缓存策略是 Cache First 还是 Network First？每种资源用的策略对吗？

---

### Day 32（周五）— 原理日：Node.js 事件循环 📖

#### 🧠 独立思考（全天，60-90 分钟）

今天不写代码，纯学原理。

**学习方式**：先自己带着代码题去推理，再运行验证

**带着这些问题去学**：

1. **事件循环的阶段**（25 分钟）：
   - Node.js 事件循环有哪 6 个阶段？每个阶段做什么？
   - `setTimeout` 在哪个阶段执行？`setImmediate` 呢？
   - 为什么 `setTimeout(fn, 0)` 和 `setImmediate(fn)` 的顺序不确定？

2. **宏任务 vs 微任务**（20 分钟）：
   - 预测下面代码的输出顺序，然后运行验证：
   ```js
   console.log('1');
   setTimeout(() => console.log('2'), 0);
   Promise.resolve().then(() => console.log('3'));
   process.nextTick(() => console.log('4'));
   console.log('5');
   ```
   - 如果答错了，搞清楚为什么

3. **为什么 Node.js 能处理高并发？**（15 分钟）：
   - 单线程怎么可能比多线程快？
   - I/O 操作（读文件、查数据库）是怎么异步的？
   - 什么操作会"阻塞事件循环"？（CPU 密集型：加密、压缩、JSON.parse 大对象）

4. **在 bookmark 中找问题**（10 分钟）：
   - 后端有没有可能阻塞事件循环的操作？
   - 如果要做一个 CPU 密集型操作（比如解析大 EPUB），应该怎么处理？

#### 🤖 AI 使用方式

- 你自己做完代码题、写完笔记后，让 AI 帮你检查理解是否正确

#### 产出

- 一篇博客笔记《Node.js 事件循环——用动图解释》
- 包含至少 3 个代码题的预测 vs 实际输出

---

### Day 33（周六）— 挑战日：设计一个 API 🧪

#### 🧠 完全独立完成（60 分钟）

**规则**：关掉所有 AI 工具。

**任务**：假设要给 bookmark 加一个"阅读统计"功能（每日阅读时长、本周阅读本数、最常读的标签等）

自己设计完整的方案：

1. **数据采集**（15 分钟）：
   - 前端上报还是后端计算？
   - 上报什么数据？格式是什么？
   - 多久上报一次？

2. **数据存储**（15 分钟）：
   - 原始事件表？聚合表？两个都要？
   - 表结构长什么样？

3. **API 设计**（20 分钟）：
   - 接口路径、方法、请求参数、响应格式
   - 至少设计 3 个接口

4. **安全和边界情况**（10 分钟）：
   - 如果用户恶意伪造阅读时长怎么办？
   - 如果用户一天上报 10000 次怎么办？

**写完后**：打开 AI，对比你的设计和 AI 的方案

---

### Day 34（周日）— 视野日 🌍

#### 🧠 独立思考

看一个技术大会演讲或技术博客：
- 推荐：React Conf / Node Congress / JSConf 的演讲录像
- 或阅读 Dan Abramov / Kent C. Dodds / Tao of Node 等高质量博客

对每个看的内容回答：
1. 核心观点是什么？
2. 有什么是你之前理解错的？
3. 能应用到 bookmark 中吗？

#### 产出

- 一篇博客笔记《本周技术阅读笔记》

---

## 第 6 周（Day 35-41）：高级功能 + 系统设计

### Day 35（周一）— 阅读统计功能实现 🔨

#### 🧠 独立思考（15 分钟）

回顾 Day 33 你设计的阅读统计方案，结合 AI 的反馈，定出最终方案。

**关键决策**：
- 事件上报用 `POST /reading-events` 还是 WebSocket？
- 统计数据是实时聚合还是定时任务聚合？

#### 🤖 AI 辅助（45 分钟）

- 让 AI 基于你的设计实现 ReadingEvent 模型和 API
- 让 AI 实现前端的阅读事件上报（页面可见时开始计时，离开时上报）
- 让 AI 实现统计查询 API（今日/本周/本月阅读时长）

#### 👀 Review 清单

- [ ] 前端上报是否做了防抖/节流？不是每秒都上报？
- [ ] 后端是否对上报频率做了限制？
- [ ] 统计查询是否用了数据库聚合函数（SUM/GROUP BY）而不是在代码里循环？
- [ ] 时区问题处理了吗？"今天"是用户的今天还是 UTC 的今天？

---

### Day 36（周二）— 导入导出功能 🔨

#### 🧠 独立思考（20 分钟）

**拆解训练**：任务是"让用户能导出和导入自己的数据"

**思考**：
- 导出什么格式？JSON 最简单，CSV 能用 Excel 打开，两个都支持？
- 如果用户有 1000 条笔记，一次性加载到内存会不会炸？（流式处理）
- 导入时如何处理数据冲突？（同 ID 的笔记已存在怎么办？）

<details>
<summary>参考拆解</summary>

```
终点：用户能下载自己的所有数据，也能从文件恢复数据

材料：
  - 导出 API：GET /export?format=json
  - 导入 API：POST /import（接收文件）
  - 导出格式定义：包含哪些数据、什么结构
  - 冲突处理策略

顺序：
  1. 定义导出数据格式（JSON Schema）
  2. 实现导出 API（流式响应，不一次性加载所有数据）
  3. 实现导入 API（事务性：要么全成功，要么全失败）
  4. 处理导入冲突（跳过/覆盖/报错让用户选）
  5. 前端下载按钮 + 导入上传组件
```
</details>

#### 🤖 AI 辅助（40 分钟）

- 让 AI 实现导出导入 API
- 让 AI 处理大数据量的流式导出

#### 👀 Review 清单

- [ ] 导出是否只导出当前用户的数据？（不要导出别人的！）
- [ ] 导入时是否验证了数据格式？（防止恶意文件）
- [ ] 大量数据导出是否使用了流式响应？
- [ ] 导入是否在事务中执行？

---

### Day 37（周三）— 邮件通知 + 定时任务 🔨

#### 🧠 独立思考（15 分钟）

**思考**：
- 什么场景需要邮件通知？（注册确认、密码重置、每周阅读报告）
- 定时任务：每周日晚上自动发送本周阅读报告，怎么实现？
- 邮件服务选什么？Resend（现代、简单）/ Nodemailer + SMTP？

#### 🤖 AI 辅助（45 分钟）

- 让 AI 配置 @nestjs/schedule 定时任务
- 让 AI 集成 Resend 发送邮件
- 让 AI 实现每周阅读报告的邮件模板（HTML 邮件）

#### 👀 Review 清单

- [ ] 邮件 API Key 是否从环境变量读取？
- [ ] 定时任务的 cron 表达式是否正确？
- [ ] 邮件发送失败是否有重试机制？
- [ ] HTML 邮件模板在不同邮件客户端显示正常吗？

---

### Day 38（周四）— 多端适配检查 🔨

#### 🧠 独立思考（20 分钟）

**检查清单**（自己过一遍）：
- 手机浏览器打开 bookmark，核心功能能用吗？
- PWA 模式下，离线能看已下载的书吗？
- 如果网络突然断了，用户正在写的笔记会丢吗？

**关键问题**：离线和在线的数据同步怎么处理？
- 离线时写入 IndexedDB
- 恢复网络后，本地数据和服务器数据可能冲突
- 最简单方案：Last Write Wins（时间戳大的覆盖）

#### 🤖 AI 辅助（40 分钟）

- 让 AI 实现离线降级逻辑（检测网络状态 → 切换数据源）
- 让 AI 实现恢复网络后的数据同步（diff + 合并）

#### 👀 Review 清单

- [ ] 离线时是否有明确的 UI 提示？（用户要知道自己离线了）
- [ ] 恢复网络后同步是否自动触发？
- [ ] 同步冲突的处理策略是否可预期？（用户不应该莫名丢失数据）

---

### Day 39（周五）— 原理日：系统设计入门 📖

#### 🧠 独立思考（全天，60-90 分钟）

**学习方式**：带着 bookmark 的架构去学系统设计

**带着这些问题去学**：

1. **系统设计框架**（15 分钟）：
   - 需求分析 → 高层设计 → 详细设计 → 瓶颈分析
   - 用这个框架重新审视 bookmark 的架构

2. **扩展性思维**（20 分钟）：
   - 如果 bookmark 有 100 用户 → 当前架构够用
   - 如果有 10000 用户 → 数据库查询变慢？加索引？读写分离？
   - 如果有 100000 用户 → 需要负载均衡？CDN？缓存层？
   - 画出三个阶段的架构图对比

3. **读写比例**（10 分钟）：
   - bookmark 是读多写少的系统（用户大部分时间在阅读，偶尔做笔记）
   - 这意味着什么？（可以重度缓存读操作、读写分离）

4. **CAP 定理**（15 分钟）：
   - 一致性、可用性、分区容忍性不可兼得
   - bookmark 应该优先保证哪个？为什么？

5. **常见架构模式**（10 分钟）：
   - 负载均衡、读写分离、消息队列、CDN
   - 知道它们存在就行，不需要现在实现

#### 🤖 AI 使用方式

- 你画完架构图后，让 AI 帮你检查遗漏

#### 产出

- 一篇博客笔记《我的第一次系统设计思考——拿 bookmark 练手》
- 包含 100/10000/100000 用户三阶段架构图

---

### Day 40（周六）— 挑战日：Code Review 🧪

#### 🧠 完全独立完成（60 分钟）

**规则**：关掉所有 AI 工具。

**任务**：
1. 选 bookmark 项目中 AI 帮你写的 3 个文件
2. 逐行 Review，列出所有问题，分类为：
   - 🔴 安全问题（必须修）
   - 🟡 性能问题（应该修）
   - 🔵 可读性问题（最好修）
   - ⚪ 风格问题（可以不修）
3. 对每个问题写出修复方案

**写完后**：打开 AI，让 AI 也 Review 同样的文件，对比你们各自发现的问题

---

### Day 41（周日）— 视野日 + 阶段二中期回顾 🌍

#### 🧠 独立思考

1. 回顾 Day 21-40 的进展，列出：
   - 3 个你掌握得不错的知识点
   - 3 个还模糊的知识点
   - 1 个完全没搞懂的知识点

2. 思考 bookmark 还缺什么功能，按优先级排序

#### 产出

- 更新博客，写一篇《第 5-6 周学习回顾》

---

## 第 7 周（Day 42-45）：收尾与沉淀

### Day 42（周一）— 性能优化实战 🔨

#### 🧠 独立思考（20 分钟）

**拆解训练**：任务是"全面优化 bookmark 的性能"

**自己列出优化清单**：
- 后端：哪些查询慢？（用 `EXPLAIN ANALYZE` 检查）加什么索引？
- 前端：首屏加载多大？哪些 JS 可以懒加载？
- 网络：图片大小？API 响应大小？能压缩吗？

#### 🤖 AI 辅助（40 分钟）

- 让 AI 在 Prisma 中添加必要的索引
- 让 AI 配置代码分割（React.lazy + Suspense）
- 让 AI 配置图片压缩和懒加载

#### 👀 Review 清单

- [ ] 慢查询是否用了 `EXPLAIN ANALYZE` 分析过？
- [ ] 代码分割后，路由切换是否有 loading 状态？
- [ ] 图片是否用了 WebP 格式 + 响应式尺寸？

---

### Day 43（周二）— 安全加固 🔨

#### 🧠 独立思考（20 分钟）

**自己做安全审查**（用 OWASP Top 10 作为清单）：
1. 所有用户输入是否做了校验？（前端 + 后端都要校验）
2. SQL 注入：Prisma 帮你防了，但 raw query 有没有？
3. XSS：React 默认转义了，但有没有用 `dangerouslySetInnerHTML`？
4. CSRF：用 JWT + Authorization Header 的话天然免疫
5. 文件上传：限制类型、大小了吗？
6. Rate Limiting：暴力破解密码能成功吗？

#### 🤖 AI 辅助（40 分钟）

- 让 AI 添加 helmet 中间件（设置安全 HTTP Header）
- 让 AI 添加 rate limiter（@nestjs/throttler）
- 让 AI 检查并修复你在审查中发现的问题

#### 👀 Review 清单

- [ ] helmet 是否配置了 CSP（Content Security Policy）？
- [ ] rate limiter 的限制是否合理？（登录接口应该比普通接口更严格）
- [ ] 环境变量中是否有硬编码的密钥？
- [ ] 错误信息是否暴露了内部实现细节？

---

### Day 44（周三）— 项目文档 🔨

#### 🧠 独立思考（20 分钟）

**思考**：如果一个新同事加入项目，他需要知道什么？
- 怎么在本地跑起来？（Docker? pnpm dev?）
- 项目结构是什么？每个目录做什么？
- 有哪些重要的技术决策？为什么这样选？

#### 🤖 AI 辅助（40 分钟）

- 让 AI 完善 README：功能说明、截图、技术栈、部署方式
- 让 AI 写一份架构文档：模块职责、数据流、技术决策记录（ADR）

#### 👀 Review 要点

- 新手看了 README 能跑起来吗？自己按照步骤试一遍
- 架构文档和实际代码是否一致？

---

### Day 45（周四~周日）— 阶段二收尾

> 和 Day 20 一样，收尾期按日历天组织，不单独编号

#### Day 45a 周四 — 修复遗留 bug

- 修复遗留 bug，确保线上版本稳定
- 补全缺失的错误处理和边界情况

#### Day 45b 周五原理日 📖

- 自选一个你在这个阶段遇到的最大难题，深入研究其原理
- 花整天时间搞清楚它

#### Day 45c 周六挑战日 🧪

- 关掉 AI，用 30 分钟画出 bookmark 的完整架构图（含前端、后端、数据库、缓存、部署拓扑）
- 然后对着架构图给一个假想的面试官讲解 5 分钟
- 录音或写成文字，找出讲不清楚的地方

#### Day 45d 周日视野日 🌍

- 写一篇《从前端到全栈：第二个月我学到了什么》
- 诚实记录：哪些掌握了，哪些还模糊，哪些完全没搞懂

---

## 阶段二自测清单

### 🧠 能独立做到
- [ ] bookmark 已实现完整的全栈功能，线上可访问
- [ ] 能解释 bookmark 的架构设计，说出每个决策的理由
- [ ] 能用 DevTools 分析并定位性能问题
- [ ] 能独立完成一个 API 的设计（数据模型 + 接口 + 安全考虑）
- [ ] 能说出 Node.js 事件循环的核心机制

### 👀 能审查 AI 的代码
- [ ] 能看出状态管理方案是否合理（什么该用 Zustand，什么该用 React Query）
- [ ] 能发现缓存策略的问题（缓存了不该缓存的、该失效没失效的）
- [ ] 能列出 bookmark 中做过的安全加固措施和原因

### 📦 有可展示的产出
- [ ] 至少 4 篇新增博客笔记
- [ ] 完整的 API 文档（Swagger）
- [ ] 清晰的架构文档
